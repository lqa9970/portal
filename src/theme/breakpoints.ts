import { BreakpointsOptions } from '@mui/material'

const breakpointSlice: BreakpointsOptions = {
  keys: ['xs', 'sm', 'md', 'lg', 'xl'],
  values: {
    xs: 0,
    sm: 600,
    md: 900,
    lg: 1100,
    xl: 1100,
  },
}

export default breakpointSlice
